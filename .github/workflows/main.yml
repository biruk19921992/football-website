name: VibeSport News Automation

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  fetch-news:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger news importer
        run: |
          curl -fsS --max-time 120 "https://footballxtra-website.onrender.com/api/news"
